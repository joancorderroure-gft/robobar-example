import { StrykerOptions } from '@stryker-mutator/api/core';
import { Logger, LoggerFactoryMethod } from '@stryker-mutator/api/logging';
import { TestRunner, DryRunOptions, MutantRunOptions, DryRunResult, MutantRunResult } from '@stryker-mutator/api/test-runner';
export declare class KarmaTestRunner implements TestRunner {
    private readonly log;
    private readonly starter;
    private exitPromise;
    private runConfig;
    static inject: ["logger", "getLogger", "options"];
    constructor(log: Logger, getLogger: LoggerFactoryMethod, options: StrykerOptions);
    init(): Promise<void>;
    dryRun(options: DryRunOptions): Promise<DryRunResult>;
    mutantRun(options: MutantRunOptions): Promise<MutantRunResult>;
    private run;
    dispose(): Promise<void>;
    private loadSetup;
    private runServer;
}
//# sourceMappingURL=karma-test-runner.d.ts.map